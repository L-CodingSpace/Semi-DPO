# Core (tested with torch 2.x, diffusers 0.31, transformers 4.57, accelerate 1.x, datasets 3-5, peft 0.13)
torch>=2.1
torchvision
diffusers>=0.25
transformers>=4.38,<5
accelerate>=0.26
datasets>=2.16
peft>=0.8
numpy
Pillow
tqdm

# Reward models (data scoring, validation and evaluation)
git+https://github.com/openai/CLIP.git
image-reward
hpsv2

# Logging and plots
wandb
tensorboard
matplotlib
